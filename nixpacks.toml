[phases.build]
cmds = [
  "npm ci",
  "npx prisma generate",
  "npm run build",
]

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "npx prisma db push --accept-data-loss && npx prisma db seed && npm run start"
