[phases.build]
cmds = [
  "npm ci",
  "npx prisma generate",
  "npm run build",
  "npx prisma migrate deploy",
  "npx prisma db seed || true"
]

[phases.setup]
nixPkgs = ["nodejs_20"]

[start]
cmd = "npm run start"
